# Stay Supplier Hub 작업 저널

> 작성 기준: `docs/ai-logs/`와 `docs/설계 논의사항/`에 남아 있는 기록을 시간순으로 다시 읽고, 최종 결정뿐 아니라 중간 주장·반박·정정·보류·구현 중 발생한 문제까지 정리했다. 설계 문서의 결정과 AI 로그의 구현 결과가 다를 때는 둘을 구분해 적었다.

## 1. 프로젝트의 출발점과 기록 원칙

이 프로젝트의 목표는 서로 다른 형식으로 숙박 상품을 제공하는 공급사 A·B를 하나의 검색 API 뒤에 통합하는 것이다. 숙소 목록은 별도로 동기화해 공급사 코드와 내부 ID를 매핑하고, 검색 시에는 날짜·인원 조건으로 재고와 요금을 두 공급사에 병렬 요청한다. 핵심 성공 조건은 단순히 정상 응답을 만드는 것이 아니라, 타임아웃·공급사별 실패·부분 결과·공급사마다 다른 성공 표현을 일관되게 처리하는 것이었다.

초기 AI 분석에서 확인한 중요한 범위는 다음과 같다.

- 필수 흐름: 매핑 저장, 공급사 어댑터, 통합 검색 API, 병렬 호출, 타임아웃, 부분 실패, 실패 판정 통일, 시연용 Mock, README.
- 선택 범위: 재시도, 서킷 브레이커, 요금·재고 캐시, 변환 실패 격리, 중복 상품 병합, 통화 처리, 예약 대행, 연동 지표, 다중 인스턴스 조율.
- 명시적 범위 밖: 인증·결제, 관리자 기능, 프론트엔드, 실제 외부 API, 지역·키워드 필터, 정렬·페이징.

AI 로그에는 요구사항 원문을 그대로 공개 산출물에 복사하지 말고, 무엇을 물었고 어떤 답을 수용·수정·거부했는지만 자신의 말로 남겨야 한다는 판단도 기록되어 있다. 이 저널은 그 원칙에 따라 대화 원문 대신 판단의 변화와 근거를 요약한다.

## 2. 시간순 작업 흐름

### 2026-09-11 — 요구사항 해석과 초기 위험 찾기

Claude Code가 요구사항 PDF 전체를 읽고 핵심 흐름, 공급사 A·B의 API 차이, 필수·선택 범위, README에 근거를 남겨야 할 결정을 정리했다. 이때부터 다음 문제가 설계 쟁점으로 올라왔다.

- A는 날짜별 세전 요금과 세금을 주고 B는 세금 포함 기간 총액을 준다. 두 공급사가 공통으로 보장하는 금액 기준이 무엇인지 정해야 했다.
- A와 B가 같은 숙소를 취급하지만 공통 식별자가 없다. 예시상 A-10023과 B77120이 같은 숙소일 수 있으나, 이를 증명할 정보가 없었다.
- 재고·요금 API는 한 번에 숙소 코드 50개까지만 받는다. 숙소가 수천 개가 되면 검색 하나가 수십 회 호출이 될 수 있다.
- B는 HTTP 200이어도 본문 `resultCode`가 실패를 나타낼 수 있다. HTTP 상태만으로는 성공을 판정할 수 없다.
- Mock이 요청 날짜와 숙소 코드를 무시하면, 실제 검증 규칙이 정상 작동해도 고정 예시와 다른 검색이 모두 빈 결과가 된다.
- 요구사항 문서·AI 로그·GSD 산출물이 공개 저장소에 섞이지 않도록 저장소 범위와 무시 파일을 먼저 조심해야 했다.

사용자는 공급사 형식과 구현 흐름을 이해하기 위한 다이어그램을 요청했고, Claude Code는 공급사 비교, 조회 흐름, 식별자, 요금, 재고, 실패 판정, Mock 구성 등을 보여 주는 HTML 자료를 만들었다. 다이어그램은 설계 이해를 돕는 보조 수단이었고, 최종 설계의 근거는 이후 `docs/설계 논의사항/`의 텍스트 기록으로 정리했다.

### 2026-09-11 ~ 2026-09-12 — 인원 사전 필터와 어댑터 경계

#### 인원 사전 필터를 넣지 않기로 함

DB에 저장한 `maxOccupancy`로 요청 인원을 수용할 숙소가 하나도 없는 경우를 공급사 호출 전에 제거할 수 있다는 제안이 나왔다. 처음에는 호출 수를 줄이는 최적화처럼 보였지만 다음 이유로 핵심 구현에 넣지 않았다.

- 현재 규모에서는 공급사별 호출 횟수가 거의 줄지 않는다.
- 동기화와 검색 사이에 객실 구성이 바뀌면 수용 가능한 숙소를 조용히 누락시킬 수 있다.
- 공급사가 이미 요청 인원을 만족하는 객실만 반환한다는 규칙을 검색 계층에서 복제한다.
- 도입 자체는 나중에 요청 비율, 공급사별 호출 수, 429 비율을 측정한 뒤 쉽게 할 수 있다.

논의 중 Claude가 “스펙의 기본 흐름과 다르다”, “공급사 규칙이 도메인으로 샌다”라고 주장했지만, 사용자가 각각 근거를 요구했다. Claude는 스펙 위반 주장을 철회했고, `maxOccupancy`가 표준 모델의 필드이므로 공급사 규칙 누수라는 표현도 수정했다. 최종 판단은 “필터가 틀렸을 때의 위험과 이득을 비교하면 지금은 이득이 작다”였다. 이 과정은 `occupancy-prefilter.md`에 남겼다.

#### 어댑터 계층의 기본 원칙

공급사별 요청·응답 변환과 실패 매핑을 어댑터 안에 가두고, 도메인은 공통 요청·응답만 보도록 정했다. 공통 인터페이스는 호출 쪽 패키지가 소유하며, 도메인이 공급사 패키지를 import하지 않도록 한다.

초기에는 공급사 형식에 없는 데이터를 담을 “추가 데이터” 칸을 결과 객체에 둘 수 있다는 생각이 있었지만, 다음 이유로 버렸다.

- 호출부가 그 칸을 읽는 순간 공급사별 형식을 알아야 하므로 계층 경계를 우회한다.
- `Map<String, Object>`나 `JsonNode`가 되기 쉬워 컴파일러가 경계 위반을 잡지 못한다.
- 실제로 필요한 데이터가 생기면 공통 타입의 선택 필드, 불투명 값, 원본 보관, 새 도메인 포트 중 목적에 맞는 경로를 고를 수 있다.

이후 공급사 응답은 `HTTP 응답 → 공급사 응답 객체 → 성공·실패 판정 → 매퍼 → 공통 형식`의 네 경계로 나누었다. 공급사 응답 객체는 패키지 내부에 두고, 매퍼는 HTTP·Reactor·DB를 모르는 순수 함수로 만든다. 이 구조는 파싱 실패와 변환 실패를 분리하고 B의 본문 코드 판정 위치를 고정한다.

### 2026-09-11 ~ 2026-09-16 — WebClient·가상 스레드·WebFlux 논쟁

#### 처음의 혼란

WebClient와 `Mono`·`Flux`의 배경지식이 부족한 상태에서, “여러 공급사 호출을 합친 뒤 마지막에 한 번 `block()`하면 내부 호출은 병렬로 진행되는가”를 먼저 확인했다. AI는 WebClient를 즉시 결과를 주는 동기 호출이 아니라 조합 가능한 작업으로 설명했다.

여기서 중요한 구분이 생겼다.

- 호출을 먼저 Reactor 연산자로 조합하고 마지막에 한 번 `block()`하면 병렬성·타임아웃·부분 실패 수집은 조합 체인에 남는다.
- 공급사 호출 하나마다 `block()`하면 호출 사이의 동시성·전체 데드라인·취소·부분 실패 수집을 애플리케이션이 다시 관리해야 한다.
- 따라서 가상 스레드는 공급사 호출을 병렬화하는 수단이 아니라, 이미 조합된 전체 결과를 MVC 요청 경계에서 한 번 기다리는 실행 기반으로 둔다.

#### AI의 잘못된 전제와 Codex의 독립 검토

Claude는 한때 “기본 아웃바운드 풀 16개이므로 Tomcat 플랫폼 스레드 200개가 구조적으로 먼저 포화되지 않는다”고 주장했다. 사용자는 기본값·임의 응답 시간만으로는 그런 병목 순서를 증명할 수 없다고 반박했다.

Codex는 별도 로그에서 이 논의를 독립적으로 검토했다.

- 기본 `HttpClient` 생성 경로와 사용자 정의 `ConnectionProvider`의 기본값을 혼동한 점을 지적했다.
- 연결 수, 진행 중인 요청 수, 풀 획득 대기 수, 요청 스레드 수를 같은 숫자로 계산할 수 없다고 정리했다.
- `flatMap(..., 8)`은 검색 한 건 안의 동시성이지 모든 사용자 요청을 합친 공급사 전역 상한이 아니라고 바로잡았다.
- WebFlux에서 JPA를 사용할 수 없다는 표현도 “불가능”이 아니라 “별도 실행 자원 격리와 복잡성 비용이 필요하다”로 정정했다.
- MVC의 블로킹이 항상 해당 요청 하나에만 영향을 준다는 표현도, Reactor 콜백 안에서 블로킹하면 이벤트 루프를 막을 수 있으므로 틀렸다고 정리했다.

이 검토의 결과, 풀 기본값과 병목 산술은 최종 선택의 근거에서 제외했다. AI가 반박을 수용했다가 다시 단정하는 과정 자체가 신뢰할 수 없는 근거 변경으로 이어질 수 있다는 점도 기록했다. 사용자는 “반박할 근거가 없으면 없다고 말하라”고 요구했고, 이후 판단은 측정되지 않은 수치와 대화상의 균형이 아니라 확인 가능한 실행 모델 차이에 한정했다.

#### 아웃바운드 풀 목표와 최종 선택

공급사 수용량을 모르는 상태에서 우리 서버의 처리 능력을 임의로 낮추지 않기로 하고, 아웃바운드 풀의 초기 상한 목표를 약 2,500개로 두었다. 이는 검증된 적정값이나 연결 선생성 수가 아니라, 부하 검증 전의 설정 목표다. 이후 공급사별 독립 풀로 해석했고, 전역 공급사 동시성·커넥션 획득 대기·OS 자원은 별도 검증 대상으로 남겼다.

최종 결정은 **MVC + 가상 스레드 + WebClient**다.

- Tomcat 요청 처리는 가상 스레드로 한다.
- 어댑터는 WebClient를 사용해 `Mono`를 반환하며 내부에서 `block()`하지 않는다.
- 검색 서비스는 Reactor로 공급사·청크 호출을 조합한다.
- 요청 경계에서 완성된 검색 결과만 한 번 `block()`한다.
- 일반 코드·JPA·매핑 DB는 MVC의 명령형 영역에 두고, 외부 호출 제어만 Reactor로 국소화한다.

WebFlux를 기술적으로 불가능하다고 본 것은 아니다. 사용자 연결 종료 취소 전파, 전 구간 non-blocking, 스트리밍이 중요해지거나 DB도 리액티브로 바꾸게 되면 다시 검토한다. 현재는 단일 JSON 응답, 동기 DB·일반 라이브러리 사용 편의와 실행 모델의 경계가 더 직접적인 이득이라고 판단했다.

### 2026-09-17 — 나머지 설계 결정을 확정

9월 17일에는 남아 있던 설계 논점을 문서별로 나눠 확정했다. 아래는 결정과 그 결정이 포기한 대안을 함께 묶은 것이다.

#### 표준 모델과 상품

| 주제 | 결정 | 포기하거나 미룬 것 | 이유 |
|---|---|---|---|
| 표준 요금 | 세금 포함 숙박 기간 전체 총액 + 통화 | A의 날짜별 요금·세금 내역, 1박 평균, 세전 기준 | B에는 날짜별·세전 정보가 없어 양쪽 공통 기준이 되지 않으며, 평균은 원본에 없는 값을 만든다 |
| 상품 구조 | 숙소 → 객실 타입 → 오퍼 배열 | 객실 타입당 오퍼 하나로 고정 | 향후 요금제·공급사별 오퍼를 담을 때 구조를 깨지 않기 위해 배열을 선택했다 |
| 숙소명·객실명·최대 인원 | 매핑 DB 값을 기본으로 채우고 조회 응답 값이 있으면 응답에서만 덮어씀 | 검색 값만 사용하거나 DB 값만 고정 | 검색 응답의 최신 사실과 재고 API에 정적 필드가 없는 공급사를 둘 다 처리하기 위해서다 |
| 조식 | 오퍼의 `breakfastIncluded` 조건으로 표시하고 가격은 보정하지 않음 | 조식 가격을 추정해 총액에서 제거, 조건 객체로 과도하게 일반화 | 조식 가격을 계산할 근거가 없고, 조건을 가격 옆에 보여 주는 편이 정확하다 |
| 연박 재고 | 요청 숙박일별 잔여 객실 수의 최솟값 | 첫날 값, 평균, 합계 | 어느 하루라도 방이 부족하면 기간 전체를 예약할 수 없기 때문이다 |
| 예약 불가 오퍼 | 오퍼를 제거하지 않고 `availableRooms: 0`으로 노출 | 0인 오퍼와 숙소를 모두 제거 | 매진 숙소와 취급하지 않는 숙소를 구분하고, 사용자가 요금·조건을 확인할 수 있게 하기 위해서다 |
| 날짜 불일치 | 요청 숙박일 집합과 다르면 해당 항목을 변환 실패로 제외 | 빠진 날을 0으로 보정, 있는 날만 계산, 초과 날짜 무시 | 공급사 의도를 추측하지 않고, 잘못된 기간의 요금·재고를 정상 데이터로 내보내지 않기 위해서다 |

#### 매핑과 숙소 목록 동기화

- 내부 식별자는 DB 시퀀스 숫자로 둔다. `(supplier, supplier_property_code)`와 `(property_id, supplier_room_type_code)` 유니크 제약과 upsert가 동일 상품의 ID 안정성을 보장한다.
- 공급사 목록에서 사라진 행은 삭제하지 않고 비활성화한다. 다시 나타났을 때 같은 내부 ID를 유지하고, 목록 API 일시 실패로 전체가 비활성화되는 사고를 줄이기 위해서다.
- 목록 동기화는 기동 시 1회, 6시간 주기, 수동 트리거, 미매핑 객실 타입 발생 시 이벤트 트리거를 조합한다. 검색마다 전체 목록을 호출하는 방식은 정적 목록과 실시간 재고의 차이를 무시하므로 제외했다.
- 동기화는 검색 응답과 분리한다. 진행 중이면 검색은 갱신 전 불변 스냅샷을 읽고 기다리지 않는다.
- 성공한 목록에 없는 숙소·객실 타입은 즉시 비활성화하되, 목록이 비었거나 30% 이상 급감하면 반영하지 않고 이상으로 기록한다. 6시간, 30%, 3분·10분·30분·90분·270분의 이벤트 대기 단계는 측정값이 아닌 정책값이다.
- 준비되지 않은 공급사는 검색 대상에서 실패로 표시한다. 준비된 공급사가 하나도 없으면 503, 이전에 유효한 매핑이 있으면 동기화 실패 후에도 이전 스냅샷을 유지한다.
- 다중 인스턴스 조율은 설계만 남기고 구현하지 않는다. 실제 구현은 단일 인스턴스이며, 필요해질 때 DB 점유·인수인계 테이블로 확장한다.

#### 중복 숙소 병합

현재 구현은 공급사가 다르면 별개의 숙소·내부 ID로 노출한다. 같은 숙소 후보 표시도 하지 않는다. 이름·객실 구성만으로 자동 병합하면 다른 도시의 동명이인이나 다른 객실 타입을 잘못 합칠 수 있고, 잘못된 병합은 “두 줄로 보이는” 문제보다 예약 대상이 달라지는 더 큰 문제를 만든다.

대신 주소·좌표가 제공된다는 전제로, 좌표 근접도·정규화한 이름·객실 구성으로 후보를 만들고, 높은 점수만 자동 확정하며 중간 점수는 사람 검토로 남기는 B 설계를 문서화했다. 관리자 기능과 실제 병합은 구현하지 않는다.

사용자 노트에 있던 “의심되는 숙소를 예약-조회-취소-조회로 확인”하는 아이디어도 폐기했다. 취소 수수료·공급사 약관·실제 고객 재고 잠식·동시 예약에 따른 오판·예약 기능이 모두 문제이기 때문이다.

#### 실패 판정과 응답 계약

실패 판정은 세 단계다.

1. HTTP 상태: A는 비-2xx를 실패로 보고, B는 HTTP 200만으로 성공 판정하지 않는다.
2. 본문 봉투: B의 성공 코드·`data` 존재 여부를 확인하고, A의 2xx 내 오류 형태도 이상 응답으로 처리한다.
3. 항목 검증: 날짜, 음수 재고, 금액·통화, 중복 키, 요청하지 않은 코드, 미매핑 객실 타입 등을 검증한다.

항목 일부만 이상이면 해당 항목만 제외하고 공급사는 실패로 올리지 않는다. 한 묶음의 항목이 전부 제외되면 그 묶음을 실패로 본다. 보정으로 값을 살리지 않고 제거하며, 이상은 로그·지표로 남기는 방향이다.

응답 계약은 다음과 같다.

- `properties → roomTypes → offers` 구조를 사용한다.
- `failedSuppliers`는 실패한 공급사 객체 목록이며, “일부 또는 전부 결과가 빠졌다”는 뜻이다.
- 일부 실패는 200으로 남은 결과를 반환한다.
- 실제 호출한 공급사가 모두 실패하면 503이다.
- 매핑된 숙소가 없어 아무 공급사도 호출하지 않았다면 200 빈 결과다.
- 오류는 RFC 9457 Problem Details(`application/problem+json`)로 통일한다.
- 실패 사유의 내부 운영 정보는 응답에 넣지 않고 로그·지표에 둔다.
- 과거 체크인일, 날짜 형식, 역전된 기간, 인원 범위는 공급사를 호출하기 전에 400으로 거절한다.
- 최대 숙박 30박, 검색 가능 기간 180일, 최대 인원 36명은 운영 측정값이 아닌 정책값으로 설정한다.
- `Retry-After`는 공급사 회복 시점을 근거 없이 클라이언트에게 지시하게 되므로 넣지 않는다.

#### 50개 분할·동시성·타임아웃

- 50개 제한은 공급사 지식이므로 도메인이 아니라 각 어댑터 안에서 처리한다.
- 검색 하나 안에서는 공급사별 최대 8개 묶음을 병렬 호출한다.
- 한 묶음만 실패하면 성공한 묶음의 오퍼는 유지하고 공급사를 `failedSuppliers`에 넣는다.
- 공급사별 커넥션 풀과 전역 상한을 분리한다. 커넥션 획득 대기를 0.5초로 제한하고, 초과 묶음은 부분 실패로 흘린다.
- 검색 전체 예산은 3초, 호출별 응답 타임아웃은 2초, 연결 타임아웃은 1초다. 이 값들은 공급사 실측이 아니라 사용자 대기 한도에서 역산한 초기 정책값이다.
- “응답 타임아웃 2초 뒤 재시도 1회가 가능하다”는 초기 계산은 3초 예산과 맞지 않아 철회했다. 이 값에서 타임아웃 호출 재시도는 사실상 어렵고, 실측이 생기면 호출별 타임아웃·전체 예산·재시도를 함께 다시 정해야 한다.
- 숙소 수천 개는 어댑터 분할과 공급사별 풀, 무상태 검색 서버와 인스턴스 증설로 대응하는 설계만 남긴다. 실제 다중 인스턴스 조율은 구현하지 않는다.

### 2026-09-17 — 스택·Mock·구현 범위 확정

스택은 Java 25, Spring Boot 4.1.x, Gradle Kotlin DSL, H2 파일 모드, Spring Data JPA + Flyway 방향으로 정했다. Java 21과 Boot 3.5.x는 안전한 기존 조합이었지만, 새 프로젝트이고 Java 25가 LTS이며 Boot 3.5 지원 종료 문제가 확인되어 최신 조합을 택했다. Boot 4의 Jackson 3·Security 7 등 변화와 인터넷 예제의 3.x 편중은 남은 비용으로 기록했다.

Mock은 역할을 나눈다.

- README 수동 시연용: 별도 Gradle 모듈, 9090 포트, 요청 날짜·숙소 코드 반영, 지연·무응답·장애·50개 초과 상황을 재현한다.
- 자동 테스트용: WireMock 또는 MockWebServer로 각 테스트가 응답을 직접 통제한다.

고정 응답 하나로 둘을 모두 해결하려던 방안은 실제 날짜·코드 검증과 충돌하고 테스트 간 상태 간섭이 생겨 포기했다. 도구가 둘로 늘어나는 비용을 알고도 재현성과 수동 확인 경로를 얻기 위해 분리했다.

구현 범위는 핵심 흐름 A와 설계 범위 B로 나눴다.

- 구현: 통합 검색 API, A·B 어댑터, 매핑 스키마·동기화, 타임아웃, 부분 실패, 실패 판정, 메모리 스냅샷, 시연용 Mock, 테스트.
- 설계만 또는 후순위: 연동 지표, 재시도·서킷 브레이커, 요금·재고 캐시, 변환 실패 격리, 중복 병합, 통화 처리, 예약 대행, 다중 인스턴스 조율.
- SpringDoc은 넣기로 했다. 자동 문서 자체보다 `availableRooms`의 비가산성, `failedSuppliers`, 0의 의미 같은 계약을 코드 옆에 두는 것이 목적이다.

초기 설계에는 “설계와 문서를 먼저 커밋한 뒤 A·B 두 브랜치로 나눈다”는 결정이 있었지만, 실제 GSD 실행은 `branching_strategy: none`과 기존 main 중심 작업 흐름 때문에 main에서 순차 실행됐다. 이 차이는 설계 결정을 몰래 바꾼 것이 아니라, 실행 오케스트레이션의 브랜치 정책이 달랐던 사례로 남긴다.

## 3. AI 활용과 역할 분담

### Claude Code — 요구사항 해석과 설계 초안

초기 Claude 세션은 다음 작업에 사용됐다.

- 요구사항 PDF 분석과 필수·선택 범위 분류
- 공급사 A·B API 비교표와 이해용 HTML 다이어그램 작성
- 인원 사전 필터, 어댑터 경계, 요금 기준, 상품 단위, 연박 재고, 매핑, 동기화, 실패 판정, 타임아웃 등 논점 발굴
- 각 논점의 선택지와 장단점 초안 제시

Claude의 초안은 그대로 채택하지 않고, 사용자가 설계 근거를 계속 검증했다. 특히 동시성·풀·스레드 설명에서 근거가 약한 주장은 철회하도록 했고, 스펙에 없는 실무 예시를 사실처럼 쓰지 않도록 확인했다. 이 과정에서 `occupancy-prefilter.md`, `async-model-choice.md`, `supplier-adapter-layer.md` 등에 최종 결정과 정정 이력이 남았다.

### Codex — 독립 검토와 기술적 오류 교정

Codex는 Claude의 비동기 모델 논의가 흔들린 상황을 별도 로그와 공식 문서로 검토했다. 담당한 역할은 결론을 대신 정하는 것이 아니라 다음을 분리하는 것이었다.

- 검증된 사실과 임의의 기본값을 분리
- 연결 수·진행 중 요청 수·스레드 수·풀 대기 수를 분리
- WebClient/Reactor가 제공하는 호출 조합 기능과 MVC/WebFlux 요청 실행 모델을 분리
- `block()`이 호출별 제어를 없애는지, 호출 사이·요청 전체 제어를 잃게 하는지를 구분

그 결과 MVC(VT on) + WebClient가 선택됐고, 어댑터의 `Mono` 반환·요청 경계 단일 `block()`이라는 구체 계약으로 좁혀졌다. 약 2,500개 풀 목표도 “검증된 성능 수치”가 아니라 초기 설정 가정으로 격하해 기록했다.

### Grok — GSD 오케스트레이션과 Phase 1~3 구현

사용자는 설계 문서를 기준으로 추가 질문 없이 GSD 전 과정을 진행하고, 가능한 한 Grok 세션만 사용하도록 요청했다. Grok은 다음 순서로 새 세션을 열었다.

`gsd-new-project → discuss/plan/execute Phase 1 → Phase 2 → Phase 3 → Phase 4`

GSD 산출물은 `.planning/`에 남았고, 설계 문서에 이미 결정된 사항은 다시 묻지 않고 컨텍스트로 전달했다. Phase 1~3의 서버 구현이 끝난 뒤 Phase 4 논의로 넘어가는 시점에 Grok 세션 사용량이 소진되어 중단됐다.

### Claude Code — 세션 인계, Phase 4, 마일스톤 검증

Grok 중단 후 Claude Code가 세션 요약과 `.planning/` 상태를 읽고 Phase 4를 인계했다.

- Phase 4 계획은 3개 플랜, 2개 웨이브로 나뉘었다.
- 04-01은 A·B 50개 분할, 공급사별 묶음 동시성 8, 묶음 부분 실패를 구현했다.
- 04-03은 두 Mock의 normal/outage/no-response/delay, 추가 숙소, 특정 코드 장애 모드를 구현하고 README 시연 명령을 갱신했다.
- 04-02는 검색 전체 예산 3초와 안전망을 연결하고 단위·통합 테스트를 보강했다.
- 계획 검증은 차단 이슈 없이 끝났지만 backstop 표현 형식에 참고 경고가 하나 있었다. 워크플로가 요구한 표현이어서 유지했다.

마일스톤 통합 점검에서 경고가 두 개 나왔다. “전 항목 제외 묶음 = 실패”는 이미 설계 문서에 고정된 규칙이므로 기술 부채로 기록하고 유지했다. 반면 “빈 정상 묶음 + 실패 묶음이면 503이 될 수 있음”은 Phase 4의 성공 판정 결정과 어긋나는 실제 결함으로 판단해 `SupplierSearchResult.anyCallSucceeded` 흐름을 고쳤다.

### 2026-09-21 — 통합 테스트 구조 개선

기존에는 A-only 통합 테스트와 A+B 통합 테스트가 별도 클래스라 B-only 경로가 비어 있었다. 처음에는 A 테스트의 이름만 바꾸는 방안이 제안됐지만, 사용자가 공급사 조합 비대칭을 계속 설명해야 하는 구조가 된다고 지적했다. 최종적으로 한 클래스 아래에 다음 세 `@Nested` 축을 만들었다.

- 공급사 A만 참가
- 공급사 B만 참가
- 공급사 A와 B 참가

이 작업 중 발생한 문제와 검증은 다음과 같다.

1. 첫 실행은 18개가 모두 400으로 실패했다. 중첩 테스트에서 바깥 `@TestConfiguration`이 자동 감지되지 않아 고정 시계가 적용되지 않았고, 실제 날짜 기준으로 과거 체크인 검증에 걸린 것이 원인이었다. 명시적 `@Import`로 고쳤다.
2. A-only 행복 경로에서 참가 설정을 제거하는 red 확인으로 숙소 수가 2개에서 3개로 늘고, 동명이인 `Riverside Hotel Seoul` 조회가 A·B 두 값을 반환하는 것을 확인했다. 격리 설정이 실제로 필요한 전제임을 검증한 뒤 원복했다.
3. B-only 봉투 실패 단언을 반대로 뒤집어, B 어댑터가 `Mono.error`가 아니라 `ChunkFailure` 값으로 실패를 반환한다는 사실을 직접 검증했다.
4. 중간 리팩터링 커밋과 B-only 테스트 커밋을 논리적으로 분리했고, 최종적으로 전체 132개 테스트가 통과했다.

## 4. 막힌 지점과 해결 또는 미해결 상태

| 시점 | 막힌 지점 | 대응 | 상태 |
|---|---|---|---|
| 9/13~9/16 | MVC·WebFlux 논의에서 기본 풀 수치와 병목 순서가 잘못 전제됨 | Codex가 공식 문서와 실행 모델을 독립 검토하고, 수치 근거를 최종 결정에서 제거 | 해결. 선택은 MVC(VT on) |
| 9/17 | 2초 호출 타임아웃과 3초 전체 예산 사이에 타임아웃 재시도 여유가 없다는 계산 오류 | “타임아웃 호출도 1회 재시도 가능” 주장을 철회하고, 실측 후 함께 재조정하기로 함 | 해결. 재시도 자체는 범위 밖 |
| 9/19 | Grok 세션 사용량 소진으로 Phase 4 직전 중단 | Claude Code가 세션 요약과 GSD 상태를 읽고 Phase 4를 인계 | 해결 |
| 9/19 | Phase 4 실행 중 Mock 수동 시연과 자동 검증의 범위가 다름 | Mock 자동 검증은 컴파일·옵션·개별 curl 확인까지, 전체 앱과 Mock 연동은 수동 시연 항목으로 분리 | 부분 해결. 수동 시연 기록을 별도로 남김 |
| 9/19 | 통합 점검에서 빈 정상 묶음이 성공으로 집계되지 않아 503 가능 | red 확인 후 `anyCallSucceeded` 조립 규칙 수정 | 해결 |
| 9/19 | Windows에서 연결 타임아웃을 결정적으로 재현하기 어려움 | 연결 타임아웃은 backstop truth로 두고, 묶음 오류 격리·응답 타임아웃·풀 대기 테스트로 주변 경로 검증 | 미해결 제약. 운영 검증 필요 |
| 9/19 | Claude 세션 사용량 제한으로 전체 페이즈 재검증 에이전트가 HTTP 429로 조기 종료 | 사용량 리셋 후 재검증을 다시 실행 | 해결 |
| 9/19 | 마일스톤 아카이브 이후 `.planning`의 `covered_files` 경로가 이전 위치를 가리켜 stale | 코드 회귀와 무관한 기존 아카이브 문제로 분리하고, 현재 범위에서 일괄 경로 수정은 하지 않음 | 미해결 기술 부채 |
| 9/21 | 통합 테스트 이동 후 중첩 설정이 상속될 것이라는 예상이 틀림 | `@Import(고정시계설정.class)`를 명시 | 해결 |
| 9/21 | red 확인용 백업 복사 명령이 실패함 | 직전 red 상태를 확인하고 필요한 부분을 수동 복구한 뒤 검증 재실행 | 해결 |

## 5. 포기·보류한 것과 이유

이 절에서 “포기”는 현재 설계에서 명시적으로 채택하지 않은 선택을 뜻하고, “보류”는 필요성은 인정하지만 이번 구현에서는 설계·재검토 조건만 남긴 항목을 뜻한다. 단순히 시간이 부족해서 뺀 목록이 아니다. 각 항목은 논의에서 사용자가 어떤 위험과 비용을 더 크게 보았는지, AI의 제안 중 무엇을 수정하거나 철회했는지, 어떤 조건이면 다시 시작할지를 함께 기록한다.

### 5.1 실행 모델과 호출 방식

#### WebFlux 전면 도입 — 선택하지 않음

최종 선택은 MVC + 가상 스레드 + WebClient다. WebFlux를 기술적으로 틀린 선택으로 판정한 것은 아니다. 두 모델의 공급사 호출 제어는 WebClient와 Reactor를 공통으로 사용하므로 `timeout`, `retryWhen`, `flatMap`, `Mono.zip`, `onErrorResume` 같은 핵심 연산자의 구현 난이도는 거의 같다는 점을 먼저 확인했다.

사용자가 WebFlux를 선택하지 않은 논리는 다음과 같다.

1. 이 API는 결과를 모두 모아 하나의 JSON으로 반환한다. 현재 스트리밍 응답이 필요하지 않다.
2. 사용자 연결 종료를 공급사 호출 취소로 곧바로 연결하는 이점은 있지만, 현재 요구사항에서 그것이 ORM·동기 라이브러리 격리 비용을 감수할 만큼 결정적인 기능 이득으로 나타나지 않았다.
3. 매핑 DB, JPA, 일반 업무 로직을 명령형 코드로 유지할 수 있는 MVC의 경계가 명확하다. 외부 공급사 호출 조합만 Reactor로 국소화하면 된다.
4. WebFlux에서 JPA를 사용할 수 없다는 초기 AI 표현은 철회됐다. 별도 실행 자원으로 격리하면 가능하지만, 그 가능성 자체가 WebFlux를 선택할 근거가 되지는 않는다. 이 프로젝트에서는 격리와 전 구간 Reactor 문맥 관리가 추가 비용이다.
5. 기존 런타임을 유지해야 하는 제약도 없고 Java 25 가상 스레드를 사용할 수 있으므로, MVC의 요청 대기 비용을 줄일 수 있었다.

따라서 WebFlux의 장점이 사라졌다고 말한 것이 아니라, **현재 흐름에서 확실한 비용은 보이는데 확실한 기능 이득은 작다**고 판단해 보류한 것이다. 다음 조건이 생기면 다시 비교한다.

- 사용자 연결 종료를 감지해 공급사 호출을 적극적으로 취소해야 한다.
- 검색 결과를 스트리밍하거나 응답을 점진적으로 전송해야 한다.
- DB까지 리액티브 접근으로 바꾸기로 한다.
- 전 구간 non-blocking 수명 관리가 운영상 핵심 요구가 된다.

#### 공급사 호출마다 `block()` — 포기

가상 스레드를 공급사 호출마다 하나씩 배정하고 `block()`하는 안은 “가상 스레드가 가벼우니 괜찮지 않은가”라는 형태로 검토했다. 그러나 이 방식은 WebClient를 사용하면서 실제 제어는 스레드·Future 쪽으로 옮기는 구조가 된다.

- 호출 내부에 붙은 타임아웃과 재시도 연산자 자체가 반드시 사라지는 것은 아니다. 이 부분을 “`block()`하면 모든 제어가 사라진다”고 표현한 AI의 초기 설명은 과장이라 철회했다.
- 대신 호출들 사이의 병렬성, 전체 검색 예산, 취소, 여러 실패를 모아 부분 결과로 만드는 규칙을 호출부가 다시 관리해야 한다.
- 공급사별 청크를 Reactor로 조합하면서 각 청크에서 다시 `block()`하면, 같은 작업을 Reactor와 스레드/Future 양쪽에서 표현하게 된다.

그래서 어댑터는 `Mono`를 반환하고, 검색 서비스가 청크·타임아웃·실패를 모두 조합한 뒤, MVC 요청 경계에서 완성된 결과에 대해서만 한 번 `block()`한다. 이 선택은 단순한 코딩 취향이 아니라 **동시성·전체 예산·부분 실패의 소유자를 하나로 유지하기 위한 경계 선택**이다.

#### MVC 플랫폼 스레드와 동기 HTTP 클라이언트 — 포기

MVC 플랫폼 스레드에서 요청 전체를 기다리는 방식은 외부 응답 지연이 플랫폼 워커 상한을 점유할 수 있어 제외했다. 다만 “기본 풀 수치 때문에 플랫폼 스레드가 반드시 구조적으로 먼저 포화한다”는 초기 AI 주장은 근거가 부족해 최종 근거에서 제거했다. 기본 연결 16개라는 전제, 사용자 검색 수와 풀 대기 수를 같은 단위로 계산한 부분, `flatMap(..., 8)`을 전역 제한으로 본 부분은 모두 수정·철회됐다.

동기 HTTP 클라이언트도 검토했지만, 공급사별 커넥션 풀 격리나 타임아웃은 다른 클라이언트에도 설정할 수 있으므로 WebClient만의 구조적 근거가 아니다. 최종적으로 WebClient를 남긴 이유는 풀 크기 때문이 아니라, 호출별 제한·재시도·취소·부분 실패를 하나의 Reactor 신호 체계에서 조합하고 코드로 드러낼 수 있기 때문이다.

#### 최종 선택 — MVC + 가상 스레드 + WebClient

대안들을 검토하고 포기·보류한 결과, 이 프로젝트가 실제로 선택한 구조는 **Spring MVC + Tomcat 가상 스레드 + WebClient/Reactor**다.

- Tomcat은 사용자 HTTP 요청을 가상 스레드에서 처리한다.
- 공급사 어댑터는 WebClient로 요청을 만들고 `Mono<SupplierSearchResult>`를 반환한다. 어댑터 내부에서는 `block()`하지 않는다.
- 검색 서비스는 A·B 공급사와 50개 단위 청크를 Reactor로 조합하고, 병렬 호출·호출별 타임아웃·부분 실패·전체 검색 예산을 관리한다.
- 최종적으로 조합된 검색 결과만 MVC 요청 경계에서 한 번 `block()`한다. 가상 스레드는 공급사 호출을 병렬화하는 도구가 아니라 이 최종 대기를 감당하는 실행 기반이다.
- 아웃바운드 커넥션 풀은 초기 목표를 약 2,500개로 두되, 이는 검증된 운영 용량이 아닌 부하 검증 전의 설정 목표로 취급한다.
- 재시도 연산자를 붙일 수 있는 경계는 마련했지만, 재시도·서킷 브레이커 자체는 이번 구현 범위에서 보류했다. 즉, “Reactor로 호출 제어를 조합한다”는 실행 모델을 선택한 것과 “재시도까지 구현했다”는 것은 다른 주장이다.

이 조합을 최종 선택한 핵심 이유는 **명령형 코드와 비동기 외부 호출 제어를 분리하면서도, 호출 사이의 동시성·실패 수집·전체 시간 예산을 하나의 Reactor 체인이 계속 소유하게 하기 위해서**다. 매핑 DB·JPA·일반 업무 로직은 MVC의 자연스러운 명령형 영역에 두고, WebClient가 필요한 공급사 연동 부분만 Reactor로 국소화한다.

### 5.2 검색 최적화와 데이터 기능

#### 인원 사전 필터 — 보류하고 구현하지 않음

매핑 DB의 `maxOccupancy`를 이용해 요청 인원을 수용할 객실이 하나도 없는 숙소를 공급사 호출 전에 제거하는 SQL은 충분히 그럴듯한 최적화였다. 사용자도 다음 반론을 제시했다.

- 객실 구성은 자주 바뀌지 않는다.
- 동기화는 사용자 검색 수가 아니라 공급사 수에 비례해 수행되므로, 목록 동기화 비용은 필터를 넣지 않아도 어차피 지불한다.
- 수용 인원이 늘어난 숙소가 다음 동기화 전까지 빠지는 문제는 치명도가 낮고, 수용 인원이 줄어든 숙소는 공급사 조회에서 다시 걸러진다.
- 공급사 규칙이 바뀌어 표준 모델과 검색 쿼리를 함께 수정해야 한다면, 그것은 피해야 할 비용이 아니라 신규 공급사 추가 시 감당할 정당한 변경 비용이다.

이 반론을 받은 뒤 AI는 “공급사 규칙이 도메인으로 샌다”는 이유를 철회했다. `maxOccupancy`는 공급사 전용 필드가 아니라 표준 모델의 필드이므로, 이 필터가 경계를 침범한다는 주장은 부정확했다. 또한 필터가 만드는 누락은 동기화 지연으로 이미 받아들이는 “신규 숙소가 다음 동기화 전까지 검색되지 않는 문제”와 같은 종류라는 점도 인정했다.

그럼에도 사용자는 넣지 않기로 했다. 결정적인 이유는 필터의 위험이 너무 커서가 아니라, **현재 규모에서 이득이 작고 이득을 측정할 데이터가 없기 때문**이다.

- 일반적인 2인 검색에서는 거의 제거되지 않는다.
- 숙소가 적은 현재 구조에서는 공급사당 호출 횟수가 필터 유무와 크게 달라지지 않는다.
- 동기화 전의 최신 상태를 알 수 없다는 이유로, 공급사에 물어보면 얻을 수 있는 정상 결과를 조용히 제거할 수 있다.
- 한 번 넣으면 “필터로 빠진 숙소 수”를 별도로 관측해야 누락 비용을 알 수 있다.

따라서 현재는 매핑된 숙소를 공급사별로 50개씩 호출하고, 인원 수용 여부는 공급사 응답 검증에 맡긴다. 다음 수치가 쌓이면 재검토한다.

- 3인 이상 검색 비율
- 필터로 제거되는 숙소 비율과 실제 공급사 응답에 나타나는 비율
- 검색 1건당 호출 수 감소량
- 공급사별 429 또는 지연 변화

#### 중복 숙소 자동 병합 — 포기하고 확정 연결 설계만 보류

두 공급사가 같은 숙소를 각자의 코드로 제공할 때, 사용자는 같은 숙소를 두 줄로 보여 주면 고객이 재고를 머릿속으로 더할 수 있다고 지적했다. 특히 “2개 → 1개 예약 → 0개”와 “2개 → 1개 예약 → 여전히 2개” 중 후자가 더 위험하므로, 같은 객실을 합쳐야 할 책임이 우리에게 있다는 논리였다. 이 문제 제기로 단순히 `availableRooms` 필드에 “오퍼끼리 더하지 말라”고 적는 것만으로는 충분하지 않다는 점도 확인했다.

반대쪽 위험은 더 무거운 것으로 평가됐다.

- 공급사 간 공통 키가 없다.
- 이름이 같은 다른 도시의 숙소를 같은 것으로 판단할 수 있다.
- 같은 숙소라도 객실 타입이 다를 수 있다.
- 잘못 합친 뒤 고객이 다른 숙소나 다른 객실을 예약하면, 단순히 두 줄로 보이는 것보다 피해가 크다.
- 공급사 채널이 재고를 공유하는지 별도 할당하는지 알 수 없어, 두 오퍼의 재고를 합산하거나 최솟값으로 줄이는 규칙도 충분한 안전을 보장하지 않는다.

사용자는 이 결론이 상식과 어긋나는 이유가 “숙소 주소조차 공급사에서 주지 않는다”는 비정상적인 전제 때문일 수 있다고 보았다. 실제 연동이라면 주소·좌표를 받아 병합하는 것이 자연스럽다는 판단에 가까웠다. 그래서 현재 스펙의 정보가 부족한 상태에서 추정 병합을 구현하지 않고, 다음처럼 두 층으로 나눴다.

- **현재 동작 A**: 공급사가 다르면 별도 숙소·별도 내부 ID로 노출하고 “같은 숙소 후보” 표시도 하지 않는다.
- **향후 설계 B**: 주소·좌표를 매핑 테이블에 저장하고, 좌표 근접도·정규화한 이름·객실 구성으로 후보를 만든다. 자동 확정과 사람 검토 구간을 나누며, 확정된 연결만 canonical property/room type으로 묶는다.

B의 실제 구현은 주소·좌표를 제공하는 공급사 스펙과 확정 연결 데이터를 먼저 확보해야 한다. 그 전까지 이름 유사도만으로 자동 병합하는 선택은 하지 않는다.

#### 예약-조회-취소로 같은 숙소를 확인 — 폐기

사용자 노트에는 공통 키가 없을 때 실제 예약을 만들고, 조회·취소 전후 재고 변화를 비교해 같은 숙소인지 확인하는 아이디어가 기록되어 있었다. 사용자 스스로도 “몹쓸 아이디어”라고 표시했으며, 논의 후 완전히 버렸다.

- 검증을 위해 실제 재고를 잠시 차감한다.
- 취소 수수료와 공급사 약관 위반 가능성이 있다.
- 공급사가 채널별 재고를 분리하면 같은 숙소여도 다른 것으로 판정할 수 있다.
- 관측 중 다른 고객의 예약이 들어오면 결과가 뒤집힌다.
- 예약 흐름 자체가 검색 프로젝트 범위 밖이다.

#### 어댑터 결과의 “추가 데이터” 슬롯 — 포기

초기에는 표준 모델로 옮기지 못한 공급사 고유 데이터를 결과 객체에 함께 실어 두자는 생각이 있었다. 사용자는 앞으로 필요한 데이터가 생길 수 있다는 확장 여지를 고려했다.

하지만 이 슬롯은 실제로 읽는 순간 공급사 형식을 호출부에 노출한다. `Map<String, Object>`나 `JsonNode`가 되면 컴파일러가 경계 위반을 막지 못하고, 어떤 데이터가 어떤 수명·보안·재처리 의미를 갖는지도 불분명해진다. 그래서 추가 데이터의 종류에 따라 경로를 나누기로 했다.

- 같은 검색 응답에서 도메인이 사용할 값이면 공통 타입의 선택 필드로 추가한다.
- 나중에 같은 공급사에 그대로 되돌려 줄 불투명 값이면 어댑터만 해석하는 값으로 운반한다.
- 분석·감사·재처리 목적이면 반환 응답이 아니라 원본 보관·격리 저장으로 보낸다.
- 예약처럼 공급사별 다른 기능이 필요하면 도메인이 새 포트 인터페이스를 정의한다.

즉, 확장성을 포기한 것이 아니라 **확장 지점을 타입 없는 우회 통로로 미리 만들지 않은 것**이다.

#### A의 날짜별 요금·세금 내역과 1박 평균 — 공통 응답에서 제외

A는 날짜별 세전 요금과 세금을 주지만 B는 세금 포함 기간 총액만 준다. 사용자는 A의 상세 정보를 그대로 살리면 공급사에 따라 고객에게 보이는 정보가 달라지고, B에서는 같은 값을 계산할 수 없다는 점을 받아들였다.

- 공통 요금은 숙박 기간 전체의 세금 포함 총액과 통화로 통일한다.
- A의 날짜별 요금·세금 내역은 버리는 데이터로 기록한다.
- B의 총액을 숙박일수로 나눈 1박 평균은 원본에 없는 추정값이다.
- 평균을 정수로 반올림하면 평균 × 박수가 총액과 맞지 않을 수 있다.
- A와 B를 비교할 때 총액끼리 비교해야 하므로, 날짜별 단가를 기준으로 삼으면 B를 왜곡한다.

이 선택은 고객에게 덜 많은 정보를 주는 대신, 공급사마다 다른 정보량을 표준 응답의 의미 차이로 만들지 않는 선택이다. 날짜별 요금이 실제 요구가 되면 선택 필드로 되살릴 수 있지만, 그때는 “A에만 값이 있는 것을 허용할지”를 별도 계약으로 다시 정해야 한다.

#### 요금·재고 캐시 — 보류

사용자 노트의 “DB 조회 시 로컬 캐싱”은 매핑 DB에 대해서는 메모리 불변 스냅샷과 동기화 후 일괄 교체로 반영했다. 반면 매번 바뀌는 요금·재고까지 캐싱하는 것은 별도 문제로 보았다.

- 재고·요금은 검색 조건과 시점에 민감해 오래된 결과를 보여 줄 위험이 있다.
- B의 `totalPrice`는 요청 기간 전체 가격이므로, 1박 단위 결과를 조합하는 단순 캐시로는 정확하지 않다.
- 캐시 키에 체크인·체크아웃·인원·공급사·숙소 코드·판매 조건을 포함해야 한다.
- 호출량 감소와 신선도·무효화 비용을 비교할 측정값이 아직 없다.

따라서 핵심 구현에서는 캐시하지 않고 매번 공급사에 조회한다. 향후 도입한다면 공급사별 TTL, 재고·가격의 허용 신선도, 부분 실패 시 캐시 사용 여부, B의 기간 총액 키 설계를 먼저 정해야 한다.

#### 최종 선택 — 매핑은 스냅샷으로, 검색 최적화와 병합은 현재 넣지 않음

이 논의 묶음의 최종 선택은 다음과 같다.

- 검색 대상은 매핑된 숙소 전체를 공급사별 50개 단위로 호출한다. `maxOccupancy` 기반 인원 사전 필터는 넣지 않는다.
- 공급사가 다르면 별도 숙소·별도 내부 ID로 응답한다. 자동 병합이나 “같은 숙소 후보” 표시는 하지 않는다.
- 공급사 고유의 추가 데이터 슬롯은 만들지 않고, 필요가 생긴 목적에 따라 공통 선택 필드·불투명 값·원본 격리·새 포트 중 하나를 선택한다.
- 요금은 세금 포함 숙박 기간 전체 총액과 통화만 표준으로 삼고, 예약 가능 수는 오퍼별 기간 내 최솟값을 사용한다. 예약 가능 수가 0인 오퍼도 응답에 남긴다.
- 매핑 정보는 동기화 후 메모리 불변 스냅샷으로 교체하지만, 요금·재고는 현재 캐시하지 않고 매 검색마다 공급사에 조회한다.
- 주소·좌표가 제공되어 확정 연결 데이터를 만들 수 있을 때만 중복 병합 설계를 다시 구현 후보로 올린다.

### 5.3 회복탄력성과 운영 기능

#### 재시도와 서킷 브레이커 — 보류

요구사항은 타임아웃과 부분 실패를 필수로 했지만 재시도와 서킷 브레이커는 선택 범위였다. 사용자는 이 기능들을 단순히 “있으면 좋은 운영 기능”으로 넣지 않고, 현재의 시간 예산·실패 분류와 함께 설계해야 하는 것으로 보았다.

특히 초기에는 “호출 응답 타임아웃 2초 후 1회 재시도할 여유가 있다”는 설명이 나왔지만, 2초 + 2초는 검색 전체 예산 3초를 넘는다. 이 계산은 틀린 것으로 정정했다. 현재 값에서 타임아웃이 난 호출을 그대로 재시도하면 전체 예산과 부분 실패 계약을 깨뜨릴 수 있다. 그래서 다음을 분리했다.

- 빠른 연결 거부·즉시 실패와 긴 응답 타임아웃을 같은 재시도 대상으로 보지 않는다.
- 재시도를 넣을 때는 실패 사유별 재시도 가능 여부, 백오프, 전체 예산 내 재시도 비용을 다시 정한다.
- 공급사별 동시 호출 상한과 재시도가 결합될 때 실제 호출률이 폭증하지 않는지 관측한다.

즉, 재시도 자체를 나쁜 기능으로 본 것이 아니라 **현재 정책값만으로 안전한 재시도 정책을 만들 근거가 없어 보류**한 것이다. 서킷 브레이커도 공급사별 실패율·회복 시간·반열림 조건을 정할 측정값이 없어 같은 이유로 미뤘다.

#### 변환 실패 데이터 격리와 원본 재처리 — 보류

현재는 날짜 불일치, 음수 재고, 이상 금액, 모르는 통화, 중복 키, 요청 외 코드, 미매핑 객실 타입을 항목 단위로 제외한다. 정상 항목까지 함께 버리지 않기 위한 결정이다. 하지만 제외된 원본을 저장해 나중에 재처리하는 기능은 구현하지 않았다.

그 이유는 다음과 같다.

- 원본 보관 기간과 개인정보·공급사 데이터 보안 기준이 정해지지 않았다.
- 어떤 변환 실패를 재처리 대상으로 삼을지, 매핑 갱신 후 자동 재처리할지 정책이 없다.
- 현재 핵심 요구는 고객 응답의 안전한 정규화이지, 데이터 품질 파이프라인 자체가 아니다.
- 격리 저장을 넣으면 저장 스키마·보관량·재처리 중복·운영 화면까지 함께 필요해진다.

그래서 지금은 이상을 로그·지표로 드러낼 자리만 남겼다. 실제 운영에서 특정 공급사의 날짜 오류나 미매핑이 반복되면 원본 샘플을 보관하고 재처리하는 별도 흐름을 만든다.

#### 연동 지표 계측 — 보류

Claude는 Micrometer 카운터를 일부 심어 공급사별 호출 수·실패 수·지연을 기록하자고 제안했다. 사용자는 지표가 유용하지 않다고 한 것이 아니라, 핵심 검색 흐름 구현과 선택 범위인 운영 계측을 한 단계에 섞지 않기로 했다.

다만 지표가 필요한 판단은 분명히 남아 있다.

- 인원 사전 필터를 도입할 만큼 3인 이상 검색이 많은가
- 공급사별 429와 풀 획득 대기가 실제 병목인가
- 미매핑 객실 타입이 정말 낮은 빈도인가
- 항목 변환 실패가 공급사별로 반복되는가
- 재시도나 서킷 브레이커를 넣을 만큼 실패가 지속되는가

따라서 계측은 “하지 않음”이 아니라 **핵심 기능과 분리해 후속 판단의 전제 데이터로 보류**한 항목이다.

#### 통화 변환 — 보류

표준 응답에는 금액과 통화를 함께 두지만, 서로 다른 통화를 하나의 비교 가능한 금액으로 변환하는 기능은 넣지 않았다. 환율 기준 시점, 환율 제공원, 반올림, 환율 조회 실패, 고객에게 원통화와 변환통화를 함께 보여 줄지에 대한 결정이 없기 때문이다. 세금 포함 기간 총액이라는 공통 금액 기준을 정하는 것과, 통화를 실제로 변환하는 것은 별개의 문제로 분리했다.

#### 예약 대행 — 범위 밖

이 프로젝트는 검색과 공급사 응답 정규화까지만 다룬다. 예약을 추가하면 공급사별 예약 키, 선택한 오퍼의 재현, 재고 재확인, 결제·취소·보상 책임, 예약 실패 보상까지 새로운 계약이 필요하다. 중복 숙소 확인을 위해 예약을 이용하지 않은 이유와도 연결된다. 검색 단계에서 다루지 않은 예약 의미를 임의로 확장하지 않았다.

#### 다중 인스턴스 동기화 조율 — 설계만 남김

수천 개 숙소를 다루는 확장 설계에서는 요청 처리 서버를 무상태로 유지하고, 매핑 DB를 공유 자원으로 두어 인스턴스를 늘리는 방향을 잡았다. 하지만 인스턴스가 여러 개가 되면 다음 문제가 추가된다.

- 모든 인스턴스가 주기 동기화를 실행하면 공급사 전체 목록 호출이 인스턴스 수만큼 늘어난다.
- 한 공급사에 대한 동기화 점유와 실패 이력을 원자적으로 조율해야 한다.
- 죽은 인스턴스의 시도를 회수하되, 늦게 살아난 인스턴스가 인수인계 후 결과를 덮어쓰지 못하게 해야 한다.
- 인스턴스별 메모리 스냅샷 교체 시점이 달라 잠시 서로 다른 검색 결과를 보게 된다.

사용자는 현재 단계에서 다중 인스턴스까지 구현할 필요는 없다고 판단했다. 대신 동기화 시도 테이블, 유기된 시도 회수, 공급사 요청 기록, 원자적 점유 같은 설계를 문서로 남겼다. 실제 구현은 단일 인스턴스로 제한했고, 이벤트 트리거의 코드별 대기 상태만 무상태 원칙을 위해 DB에 둔다.

#### 지역·키워드 필터, 정렬·페이징, 관리자 기능 — 범위 밖

공급사 스펙에 지역 공통 정보가 없고, 현재 목표는 통합 호출·정규화·부분 실패다. 지역·키워드 필터를 넣으면 공급사별 검색 지원 범위, 위치 데이터, 필터 적용 순서가 추가로 필요하다. 정렬·페이징은 통합 결과의 안정적인 정렬 키와 전체 결과 수집 전략이 필요하다. 관리자 기능은 숙소 매핑 확정과 중복 병합을 실제 운영에 노출하는 별도 제품 영역이다. 따라서 이 기능들을 “나중에 쉽게 추가할 확장점”으로 과장하지 않고 요구 범위 밖으로 두었다.

#### `Retry-After` 헤더 — 포기

공급사 장애가 발생했을 때 클라이언트에게 재시도 시점을 알려 주는 헤더를 둘 수 있었다. 하지만 공급사의 회복 시점을 알 수 없는 상태에서 고정값을 넣으면, 실제 근거가 없는 행동을 클라이언트에게 강제하게 된다. 또한 모든 503이 같은 회복 시간을 갖는 것도 아니다. 클라이언트 요청 제한이나 재시도 정책을 도입할 때 공급사별 회복 신호·백오프·중복 요청 방지까지 함께 정한 후 다시 검토한다.

#### 최종 선택 — 타임아웃·부분 실패·단일 인스턴스까지만 핵심 구현

이 논의 묶음의 최종 구현 경계는 **검색 요청을 제한된 시간 안에 끝내고, 실패를 값으로 수집해 가능한 결과를 반환하는 것**이다.

- 검색 전체 예산 3초, 호출별 응답 타임아웃 2초, 연결 타임아웃 1초, 커넥션 획득 대기 0.5초를 설정값으로 둔다.
- 공급사별 50개 청크와 검색 내 동시성 8을 사용하고, 한 묶음이 실패해도 성공한 묶음의 결과는 유지한다.
- 일부 공급사 실패는 200과 `failedSuppliers`로 반환하고, 실제 호출한 공급사가 모두 실패하면 503으로 반환한다.
- 항목 단위 정규화 실패는 해당 항목만 제외하고, 묶음 전체가 유효한 결과를 내지 못하면 묶음 실패로 기록한다.
- 어댑터는 `Mono`를 반환하고, MVC 요청 경계에서 조합된 결과를 한 번 기다린다.

반대로 재시도·서킷 브레이커·원본 격리·Micrometer 지표·통화 변환·예약 대행은 이 핵심 경계에 포함하지 않는다. 다중 인스턴스 조율도 구현하지 않고 단일 인스턴스로 둔다. 즉, 이번 선택은 “운영 기능을 전부 포기”한 것이 아니라 **현재 장애를 제한하고 부분 결과를 보존하는 최소 운영 계약을 먼저 구현하고, 측정값이 필요한 기능은 후속 설계로 분리한 것**이다.

### 5.4 결정은 유지했지만 경고를 받고도 바꾸지 않은 항목

#### “전 항목 제외 묶음 = 실패” — 현행 유지, 후속 방향만 보류

50개 분할을 구현한 뒤, 한 묶음의 항목이 모두 검증에서 제외되면 그 묶음을 실패로 보는 기존 규칙에 문제가 제기됐다. 날짜 불일치·음수 재고·이상 금액·중복 키·요청 외 숙소·인원 미달은 공급사 규약 위반이므로 이 규칙이 자연스럽다. 그러나 **매핑에 없는 객실 타입 코드**는 공급사가 잘못한 것이 아니라 우리 매핑 스냅샷이 늦은 경우일 수 있다.

선택지는 세 가지였다.

1. 현행대로 그 묶음을 실패로 보고 `failedSuppliers`에 넣는다.
2. 미매핑만 남은 묶음은 정상 빈 결과로 본다.
3. 공급사 실패 목록에는 넣되, 전체 503 승격에서는 제외한다.

사용자는 1번을 유지했다.

- 매핑되지 않은 객실 타입은 유효한 내부 객실인지 아직 검증되지 않았다.
- 확실하지 않은 항목을 “제공할 수도 있는 숙소”처럼 응답에 반영하면 사용자에게 더 혼란스러운 정보를 준다.
- 정상 오퍼가 하나라도 있으면 미매핑 항목은 어차피 조용히 빠진다. 미매핑 항목만 남은 경우에도 같은 제외 원칙을 적용하는 편이 일관된다.
- 한 50개 묶음 전체가 미매핑만 되는 상황은 공급사 객실 코드 체계 개편 정도의 낮은 빈도이고, 이벤트 동기화가 다음 회복 경로를 제공한다.

다만 이 결정은 “우리 매핑이 늦었는데 공급사 실패로 보이는 어긋남”을 의도적으로 받아들인다. 그래서 다음 방향을 설계만 남겼다.

- 요청 안에서 매핑 동기화를 즉시 실행한다.
- 갱신된 매핑으로 문제 항목을 재검증한다.
- 해소되면 해당 검색에 포함하고, 끝내 해소되지 않으면 실패로 남긴다.
- 반복되는 미매핑 코드에는 코드별 쿨타임과 발동 전 찬스 횟수를 둔다.

이 방향을 바로 구현하지 않은 이유는 응답 예산을 늘려야 하고, 목록 API에 계속 나타나지 않는 코드가 갱신 부하와 사용자 지연만 키울 수 있기 때문이다. 미매핑 빈도와 공급사 목록·재고 API 간 불일치가 먼저 관측되어야 한다.

### 5.5 기능이 아니라 실행·검증 과정에서 보류된 것

- 설계 문서에는 먼저 커밋한 뒤 A/B 구현 브랜치로 나누는 흐름이 적혀 있었지만, 실제 GSD 실행은 `branching_strategy: none`에 따라 main에서 순차 진행됐다. 핵심 기능을 버린 것은 아니며, 실행 오케스트레이션의 브랜치 정책이 설계 문서와 달랐던 사례다.
- 약 2,500개 아웃바운드 풀은 초기 목표로만 남아 있다. 목적지별·공급사별 적용 단위, FD·포트·direct memory·프록시/NAT 한도, 실제 처리량 개선 여부는 부하 검증 전이다. 숫자를 확정된 운영 용량으로 취급하지 않는다.
- Windows에서 연결 타임아웃을 일정하게 재현하기 어려워, 관련 검증은 backstop truth와 주변 오류·응답 타임아웃·풀 대기 테스트로 남겼다.
- Mock의 무응답·장애 모드는 자동 테스트와 수동 시연의 역할을 나눴다. Mock 자체의 모든 동작을 애플리케이션 통합 테스트로 자동화하지 않고, 컴파일·옵션·개별 HTTP 확인과 실제 앱을 띄운 수동 시연으로 나누었다.
- v1.0 아카이브 뒤 `.planning`의 `covered_files` 경로가 이전 위치를 가리켜 전체 fingerprint가 stale이 된 문제는 제품 코드의 회귀가 아니어서 이번 범위에서 일괄 수정하지 않았다. 현재 코드의 재검증 결과와 planning 산출물의 경로 건강성을 별개 문제로 남겼다.

## 6. 구현 결과와 설계의 경계

AI 로그상 v1.0 마감 시점에 확인된 구현 결과는 다음과 같다.

- Phase 1~4가 완료되었고, 최신 HEAD 재검증에서 네 페이즈 모두 통과했다.
- 전체 테스트는 9월 19일 재검증 기준 127개 통과, 이후 9월 21일 통합 테스트 축을 보강한 뒤 132개 통과였다.
- A·B 어댑터는 숙소 코드를 50개 단위로 나누고 공급사별 최대 8개 묶음을 병렬 호출한다.
- 부분 실패에서는 성공한 묶음의 결과를 유지하고 실패 공급사를 표시한다. 실제 오퍼를 낸 묶음이 없어도 정상적으로 끝난 빈 묶음은 성공 호출로 세도록 수정했다.
- 검색 전체 예산은 3초, 호출별 응답 타임아웃은 2초, 연결 타임아웃은 1초, 풀 획득 대기는 0.5초 설정 방향이다.
- Mock A·B는 정상·장애·무응답·지연·추가 숙소·특정 코드 장애를 재현할 수 있다.
- 실제 jar와 Mock을 띄운 수동 확인에서 A의 429,000원·재고 1, 재고 0 오퍼 유지, B의 452,000원, 동일 이름 숙소의 별도 내부 ID, 120개 추가 숙소에서의 50개 분할을 확인했다.
- A 무응답은 호출별 2초 타임아웃으로 약 2.1초에 200, A·B 모두 무응답이면 약 2.1초에 503이 되었다. 3초 전체 예산이 직접 끊는 경로는 테스트로 확인했으며, 수동 시연 결과와 혼동하지 않는다.
- 마일스톤 감사의 상태는 `tech_debt`였다. 이는 차단 이슈가 있다는 뜻이 아니라, 선택 범위·검증 한계·후속 개선점을 기록한 채 마감했다는 뜻이다.

반대로 다음은 설계 문서에만 있거나 아직 운영 검증 전인 내용이다.

- 약 2,500개 아웃바운드 풀의 실제 적정성
- 공급사별 실제 수용량·429 대응·전역 호출률 정책
- 다중 인스턴스 동기화 조율
- 중복 숙소 병합
- 인원 사전 필터의 효과
- 재시도·서킷 브레이커·캐시·지표·격리 저장
- 연결 타임아웃의 Windows 결정적 재현
- `.planning` 아카이브 경로를 바로잡은 뒤의 전체 fingerprint 재계산

## 7. 근거 문서

### 설계 논의

- [`사용자 노트.md`](<docs/설계 논의사항/사용자 노트.md>)
- [`async-model-choice.md`](<docs/설계 논의사항/async-model-choice.md>)
- [`occupancy-prefilter.md`](<docs/설계 논의사항/occupancy-prefilter.md>)
- [`supplier-adapter-layer.md`](<docs/설계 논의사항/supplier-adapter-layer.md>)
- [`supplier-registration.md`](<docs/설계 논의사항/supplier-registration.md>)
- [`price-basis.md`](<docs/설계 논의사항/price-basis.md>)
- [`offer-unit.md`](<docs/설계 논의사항/offer-unit.md>)
- [`stay-availability.md`](<docs/설계 논의사항/stay-availability.md>)
- [`search-response-contract.md`](<docs/설계 논의사항/search-response-contract.md>)
- [`mapping-schema.md`](<docs/설계 논의사항/mapping-schema.md>)
- [`property-list-sync.md`](<docs/설계 논의사항/property-list-sync.md>)
- [`chunking-and-scale.md`](<docs/설계 논의사항/chunking-and-scale.md>)
- [`failure-detection.md`](<docs/설계 논의사항/failure-detection.md>)
- [`chunk-all-excluded-failure.md`](<docs/설계 논의사항/chunk-all-excluded-failure.md>)
- [`timeouts.md`](<docs/설계 논의사항/timeouts.md>)
- [`outbound-pool-capacity.md`](<docs/설계 논의사항/outbound-pool-capacity.md>)
- [`mock-setup.md`](<docs/설계 논의사항/mock-setup.md>)
- [`implementation-scope.md`](<docs/설계 논의사항/implementation-scope.md>)
- [`stack.md`](<docs/설계 논의사항/stack.md>)
- [`duplicate-merge.md`](<docs/설계 논의사항/duplicate-merge.md>)

### AI 작업 로그

- 2026-09-11 [`작업공간과 저장소 구조 설정`](<docs/ai-logs/claude-code-2026-09-11T15-27-22.435-KST-작업공간과-저장소-구조-설정.md>), [`인원 사전필터와 공급사 통합계층 설계`](<docs/ai-logs/claude-code-2026-09-11T15-27-22.435-KST-인원-사전필터와-공급사-통합계층-설계.md>), [`공급사 API 다이어그램`](<docs/ai-logs/claude-code-2026-09-11T15-27-22.435-KST-작업공간-구성과-공급사-API-다이어그램.md>)
- 2026-09-11 [`비동기 모델 논의 되돌리기`](<docs/ai-logs/claude-code-2026-09-11T15-27-22.435-KST-작업공간-구성과-비동기-모델-논의-되돌리기.md>), [`MVC·WebClient·WebFlux 논의`](<docs/ai-logs/claude-code-2026-09-11T15-27-22.435-KST-MVC-WebClient-WebFlux-비동기-모델-논의.md>)
- 2026-09-14 [`Codex MVC·가상 스레드·WebClient 선택 검토`](<docs/ai-logs/codex-2026-09-14T00-34-18.832-KST-MVC-가상스레드-WebClient-선택.md>)
- 2026-09-17 [`잔여 설계 결정과 프로젝트 초기화`](<docs/ai-logs/claude-code-2026-09-17T00-13-05.466-KST-잔여-설계-결정과-프로젝트-초기화.md>)
- 2026-09-19 [`Grok GSD 서버 구현 과정`](<docs/ai-logs/grok-2026-09-19T01-02-19.864-KST-GSD-서버-구현-과정.md>)
- 2026-09-19 [`Phase 4 실행과 v1.0 마일스톤 마감`](<docs/ai-logs/claude-code-2026-09-19T09-55-47.373-KST-Phase-4-실행과-v1.0-마일스톤-마감.md>)
- 2026-09-21 [`기존 작업 정리와 공급사 청크 실패 논의`](<docs/ai-logs/claude-code-2026-09-21T02-23-20.265-KST-기존-작업정리와-공급사-청크-실패-논의.md>)
- 2026-09-21 [`검색 통합 테스트 비대칭 해소`](<docs/ai-logs/claude-code-2026-09-21T03-40-46.655-KST-검색-통합-테스트-비대칭-해소.md>)

이 문서는 위 자료의 요약이며, AI가 제안한 모든 내용이 채택되었다는 뜻은 아니다. 명시적으로 철회·보류·범위 밖으로 둔 항목은 각 절에 따로 표시했다.
