`claude-code-2026-09-21T02-23-20.265-KST-기존-작업정리와-공급사-청크-실패-논의.md`에서 이어짐.

[2026-09-21 03:40:46.655 KST] 사용자

```text
왜 StaySearchAIntegrationTest만 있고 StaySearchBIntegrationTest는 없는지, 그리고 StaySearchABIntegrationTest는 뭔지 설명
```

---

[2026-09-21 03:42:26.908 KST] claude code

````text
세 파일의 내력과 역할이 다릅니다. 순서대로 설명하겠습니다.

## StaySearchAIntegrationTest — Phase 2의 "그 통합 테스트"

`f621ab8` (09-19 04:55, Phase 2)에 생겼습니다. **그때는 공급사가 A뿐이었습니다.** B 어댑터도, B Mock도 없던 시점이라 이 클래스가 이름만 "A"일 뿐 사실상 유일한 검색 통합 테스트였습니다. 초기 케이스가 그걸 보여줍니다.

- `A검색_세금포함총액과_기간최소재고` — 429000원, 재고 1
- `재고0_오퍼유지와_이름인원덮어쓰기`

Phase 3에서 B가 들어오면서 이 클래스는 **"A만 참가하는 경우" 전용**으로 용도가 바뀌었고, Phase 2 코드 리뷰의 치명 지적(CR-01: A의 HTTP 실패가 503이 아니라 500이 되던 문제) 회귀 테스트 4건이 여기 붙었습니다.

```java
private void A만_참가시킨다() {
    snapshotHolder.replace(MappingStore.MappingSnapshot.explicit(
            snapshotHolder.current().properties(),
            Set.of(new SupplierId("A")),   // ready = A만
            Set.of()));
}
```

스냅샷을 `EXPLICIT ready={A}`로 바꿔서 B는 아예 호출되지 않게 만듭니다. "호출한 공급사가 전부 실패 → 503"을 **A 하나만으로** 검증해야 CR-01이 잡히기 때문입니다. 참고로 이 클래스에선 B의 `base-url`도 A Mock을 가리킵니다(`StaySearchAIntegrationTest.java:149`) — B를 안 쓰니 그냥 같은 서버를 꽂아둔 겁니다.

## StaySearchABIntegrationTest — Phase 3의 부분 실패 행렬

`60773c2` (09-19 08:33, Phase 3). MockWebServer를 **둘** 띄워 A·B를 각각 꽂습니다. 여기가 B 관련 HTTP 경로 검증이 전부 모여 있는 곳입니다.

| 케이스 | 기대 |
|---|---|
| A 429000 + B 452000이 서로 다른 `propertyId` | 200 |
| B만 `resultCode: E503` (봉투 실패) | 200 + `failedSuppliers: [B]` |
| B만 비-2xx | 200 + `failedSuppliers: [B]` |
| B만 비-JSON | 200 + `failedSuppliers: [B]` |
| A·B 둘 다 실패 | 503 problem+json, `Retry-After` 없음 |
| A만 HTTP 500, B 성공 | 200 + `failedSuppliers: [A]` |
| 매핑이 없어 아무도 호출 안 함 | 200 빈 골격 |

Phase 3의 목표 자체가 "두 공급사를 병렬로 모으고 실패를 200/503으로 나눈다"였습니다. **B에 관해 검증할 가치가 있는 건 B 혼자일 때가 아니라 A와 섞였을 때입니다** — 한쪽이 죽어도 나머지 결과가 살아남는지가 요구사항(SRCH-05)이니까요. B 단독 성공 본문은 AB 행복 경로에 이미 들어 있고, B 응답 파싱·매핑 자체는 `SupplierBMapperTest`(단위)와 `SupplierBPropertiesTest`가 맡습니다.

## 그래서 StaySearchBIntegrationTest가 없는 이유

정리하면 **A 클래스가 특별해서지, B가 홀대받아서가 아닙니다.** A-only 클래스는 "A만 다루는 테스트를 두자"고 설계한 결과가 아니라, **B가 존재하기 전에 태어난 파일이 그대로 남아 A-only 자리를 물려받은 것**입니다. 대칭을 맞추려고 B-only를 새로 만들면 AB 클래스와 케이스가 겹칩니다.

## 다만 한 군데는 진짜로 비어 있습니다

A쪽 CR-01 테스트는 HTTP 응답만 보는 게 아니라 **어댑터를 직접 호출해서** 단언합니다.

```java
SupplierSearchResult 어댑터결과 = 공급사A가용성().fetchAvailability(A검색조건()).block();
assertThat(어댑터결과.failures(), not(empty()));   // Mono.error가 아니라 failures()로 돌아왔는지
```

`fetchAvailability`를 직접 부르는 테스트는 저 한 줄(`StaySearchAIntegrationTest.java:292`)이 전부고, **B에는 대응하는 단언이 없습니다.**

이게 왜 문제냐면 — `StaySearchService.java:98`에 `onErrorResume` 안전망이 있습니다. 만약 B 어댑터가 실패를 `failures()`로 바꾸지 않고 `Mono.error`를 흘려도, 안전망이 받아서 B를 실패로 기록합니다. 그러면 HTTP 응답은 `200 + failedSuppliers: [B]`로 **똑같이 나옵니다.** 즉 AB 통합 테스트만으로는 "B 어댑터가 제대로 변환했다"와 "B 어댑터가 터졌는데 안전망이 덮었다"를 **구분하지 못합니다.**

CR-01은 원래 어댑터가 `Mono.error`를 흘리던 게 원인이었고 A·B 양쪽을 고쳤는데, 회귀를 막는 단언은 A쪽에만 있는 셈입니다. 한 줄이면 메워집니다.

```java
SupplierSearchResult B결과 = 공급사B가용성().fetchAvailability(B검색조건()).block();
assertThat(B결과.failures(), not(empty()));
```

기존 "B만 `resultCode: E503`" 테스트에 얹으면 되고, 새 클래스는 필요 없습니다. 넣어둘까요?
````

---

[2026-09-21 03:44:50.300 KST] 사용자

```text
StaySearchAIntegrationTest를 없애고 StaySearchABIntegrationTest에 통합해도 문제 없는지
```

---

[2026-09-21 03:45:00.379 KST] claude code

```text
좋은 질문입니다. 합칠 수 있는지는 설정과 상태 초기화가 호환되는지에 달렸으니, 양쪽 fixture를 직접 보겠습니다.
```

---

[2026-09-21 03:47:23.172 KST] claude code

````text
기술적으로는 됩니다. 다만 공짜가 아니고, **세 군데를 반드시 손봐야 하는데 그중 둘은 지금 통과하는 테스트의 의미를 건드립니다.**

## 합쳐도 되는 부분

**격리 장치가 데이터와 무관하게 동작합니다.** `A만_참가시킨다()`는 스냅샷을 `EXPLICIT ready={A}, unready={}`로 바꿉니다. `MappingStore.java:194`의 판정이

```java
case EXPLICIT -> readySupplierIds.contains(...) || unreadySupplierIds.contains(...);
```

이라 B는 **참가 자체를 안 합니다** — 호출도 안 되고 `failedSuppliers`에도 안 들어갑니다. `activePropertyCodes(supplier)`가 공급사로 필터하므로, 스냅샷에 B 숙소가 섞여 있어도 A 호출에는 A 코드만 나갑니다.

**상태 누수도 없습니다.** 양쪽 `@BeforeEach`가 `mappingUpsertService.apply(catalog)`를 부르고, 그게 스냅샷을 `ALL_READY`로 갈아끼웁니다. `A만_참가시킨다()`의 변경은 다음 테스트 시작 시 자동으로 리셋됩니다.

**설정도 호환됩니다.** 고정 시계(2026-09-19 Asia/Seoul), 타임아웃(200ms), 검색 구간(2026-09-20~23)이 전부 같습니다. A fixture는 **필드 하나만 빼고 바이트 단위로 동일**합니다. datasource 이름은 클래스마다 유일해서 하나 사라져도 충돌 없습니다.

## 반드시 손봐야 하는 세 군데

**① `$.properties.length()` 단언이 깨집니다 — 확실한 실패**

`재고0_오퍼유지와_이름인원덮어쓰기`가 `.andExpect(jsonPath("$.properties.length()").value(2))`를 겁니다(`StaySearchAIntegrationTest.java:244`). 지금은 A 카탈로그만 적재돼 숙소가 2개입니다. AB로 옮기면 `@BeforeEach`가 B 카탈로그까지 적재하고 `ALL_READY`이므로 B77120까지 나와 **3개**가 됩니다.

**② 더 나쁜 쪽 — 동명이인 때문에 조용히 틀린 값을 봅니다**

B fixture의 `propertyName`이 **`"Riverside Hotel Seoul"`로 A-10023과 같습니다.** 의도적입니다 — "같은 호텔이 공급사가 다르면 다른 ID로 나온다"를 AB 테스트가 검증하니까요. 그런데 A 테스트의 헬퍼는 이렇게 생겼습니다.

```java
Object value = JsonPath.read(json, "$.properties[?(@.propertyName == '" + 숙소명 + "')]." + 상대경로);
if (value instanceof List<?> list) { return (T) list.getFirst(); }
```

이름으로 찾아서 **첫 번째를 집습니다.** B가 참가하면 "Riverside Hotel Seoul"이 둘이 되고, `getFirst()`가 B를 집으면 `totalPrice`가 429000이 아니라 **452000**이 나옵니다. 순서는 보장되지 않으니 환경 따라 흔들립니다.

두 문제 다 해법은 같습니다 — **A 행복 경로 두 테스트에도 `A만_참가시킨다()`를 붙이는 것.** 그러면 숙소 2개, 동명이인 없음으로 돌아옵니다.

다만 이건 **통과하는 테스트의 전제를 바꾸는 일**입니다. 지금은 "ALL_READY인데 A 카탈로그만 있는 상태", 합친 뒤엔 "EXPLICIT ready={A}인 상태". 단언하는 내용은 같지만 통과 경로가 달라지므로, 리팩터링하면서 테스트를 슬쩍 약하게 만드는 전형적인 위험 구간입니다.

**③ `"Old Name"` fixture를 지켜야 합니다**

A 카탈로그의 A-10044는 `"Old Name"`, AB 카탈로그는 `"Namsan Garden Stay"`입니다. 이 차이가 `재고0_오퍼유지와_이름인원덮어쓰기`의 **존재 이유**입니다 — 매핑 DB엔 "Old Name"이 남아 있고 검색 결과만 "Namsan Garden Stay"로 덮인다는 걸 JDBC로 직접 확인합니다(`매핑숙소명`, `스냅샷매핑.propertyName()`).

확인해 보니 **AB 테스트는 A-10044 이름을 단언하지 않습니다** — fixture에만 등장합니다. 그래서 합칠 때 `"Old Name"`을 채택하면 양쪽 다 안전합니다. 반대로 무심코 AB 쪽 fixture를 남기면 덮어쓰기 테스트가 아무것도 증명하지 않게 됩니다.

## 손익

**얻는 것**
- Spring 컨텍스트 하나 절약. 두 클래스는 프로퍼티가 달라 캐시 키가 갈리므로 지금 각각 부팅합니다. `@SpringBootTest` 클래스가 9개니 9개 중 1개를 줄이는 셈 — 의미는 있지만 극적이진 않습니다.
- A fixture 중복(숙소목록·재고요금 본문 두 벌) 제거.
- 앞서 말한 **B 어댑터 직접 단언 공백**을 메우기 쉬워집니다. A 클래스만 `availabilityPorts`를 주입받고 있는데, 합치면 한 곳에서 A·B 양쪽 `fetchAvailability`를 단언할 수 있습니다.

**잃는 것**
- 클래스 이름이 "A만 참가"라는 축을 드러내던 걸 잃습니다. `@Nested`로 "A만 참가" / "A와 B 참가"를 나누면 회복됩니다.
- 850줄, 13케이스 한 클래스가 됩니다.
- 컨텍스트 기동 실패 시 13개가 한꺼번에 죽습니다. (사소)

## 제 추천

**합치는 것 자체는 반대하지 않지만, 지금 할 일로는 우선순위가 낮다고 봅니다.** ①②를 고치려면 통과 중인 테스트의 전제를 바꿔야 하고, 얻는 건 컨텍스트 하나입니다. v1.0 태그를 찍은 직후라 더 그렇습니다.

"왜 A만 있고 B는 없지?"라는 혼란이 동기라면, **이름만 바꾸는 게 훨씬 싼 해법**입니다.

```
StaySearchAIntegrationTest → StaySearchAOnlyIntegrationTest
@DisplayName("공급사 A만 참가하는 검색")
```

한 줄짜리 커밋이고, 테스트 로직을 전혀 안 건드리면서 "A 전용 클래스가 아니라 A-only 시나리오 클래스"라는 의도를 이름이 말해 줍니다. 앞서 짚은 B 어댑터 단언 공백은 AB 클래스에 두 줄 추가로 따로 메우면 됩니다.

합치는 쪽으로 가시겠다면 `@Nested` 두 개로 나누고 ①②③을 다 반영해서 작업하겠습니다. 어느 쪽으로 할까요?
````

---

[2026-09-21 03:48:50.862 KST] 사용자

```text
비대칭인게 거슬려. Aonly만 있는 이유를 계속 설명해야 하는 운명에 빠지잖아.
```

---

[2026-09-21 03:49:51.570 KST] claude code

````text
그게 핵심이죠. 이름만 바꾸면 비대칭이 **더 잘 보이게** 될 뿐입니다 — "A-only 시나리오 클래스는 있는데 B-only는 왜 없나"로 질문이 바뀌는 것뿐이니까요. 제 앞 답변의 추천을 철회합니다.

비대칭을 진짜로 없애는 방법은 둘입니다.

**B-only 클래스를 새로 만든다** — 대칭은 맞지만 Spring 컨텍스트가 하나 더 늘고 fixture가 세 벌이 됩니다. 비대칭을 없애려고 중복을 늘리는 거래라 좋지 않습니다.

**한 클래스로 합치고 축을 `@Nested`로 드러낸다** — 이게 맞다고 봅니다.

```
StaySearchSupplierIntegrationTest  @DisplayName("공급사 검색 연동")
├── @Nested 공급사_A만_참가
├── @Nested 공급사_B만_참가
└── @Nested 공급사_A와_B_참가
```

세 축이 나란히 있으면 "왜 A만"이라는 질문 자체가 안 생깁니다. 비대칭이 클래스 구조에서 사라지고, 나중에 공급사 C가 붙어도 `@Nested` 하나 더 얹으면 끝입니다. `@NestedTestConfiguration`의 기본값이 `INHERIT`이라 `@SpringBootTest`·`@DynamicPropertySource`·`@TestConfiguration`을 그대로 물려받고 **컨텍스트는 하나**입니다. 바깥 `@BeforeEach`도 중첩 테스트에 그대로 적용됩니다.

## 구체적으로 무엇이 달라지나

**`공급사_A만_참가`** — 기존 A 클래스 6케이스를 그대로 옮기고, 앞서 짚은 세 군데를 반영합니다.
- 행복 경로 두 케이스에 `A만_참가시킨다()` 추가 (숙소 개수 2 단언과 "Riverside Hotel Seoul" 동명이인 문제 해소)
- 카탈로그 fixture는 `"Old Name"` 쪽을 채택 (덮어쓰기 검증이 의미를 유지, AB는 이 이름을 단언하지 않으므로 안전)

**`공급사_A와_B_참가`** — 기존 AB 7케이스 그대로. 손댈 것 없습니다.

**`공급사_B만_참가`** — 새로 씁니다. A-only를 거울로 뒤집되, 중복은 피합니다.

| 케이스 | 이유 |
|---|---|
| B 단독 검색이 452000·재고 1·`X-Api-Key`를 맞게 보낸다 | A-only의 행복 경로 대칭 |
| B만 참가 + `resultCode: E503` → 503, **어댑터 직접 단언** | B 고유의 봉투 실패. 지금 어디에도 없는 경로 |
| B만 참가 + HTTP 비-2xx → 503 | CR-01 회귀의 B판 |
| B만 참가 + 비-JSON → 503 | 〃 |
| B만 참가 + 응답 지연 → 503 | 〃 |

이름·인원 덮어쓰기와 재고 0 유지는 **공급사와 무관한 공통 매핑 로직**이라 A쪽에서 한 번만 검증합니다. 이건 설명 가능한 비대칭입니다 — "공통 로직은 한 번"이니까요.

그리고 이게 앞서 짚은 공백을 메웁니다. 지금 `fetchAvailability`를 직접 단언하는 곳은 `StaySearchAIntegrationTest.java:292` 한 줄뿐이라, AB 테스트만으로는 **"B 어댑터가 실패를 `failures()`로 변환했다"와 "B 어댑터가 `Mono.error`를 흘렸는데 `StaySearchService.java:98`의 `onErrorResume` 안전망이 덮었다"를 구분하지 못합니다.** B-only 축이 생기면 자연스럽게 메워집니다.

## 커밋 단위

AGENTS.md대로 섞지 않고 나눕니다.

1. `♻️ refactor: 공급사 검색 통합 테스트를 참가 조합 축으로 합친다` — A/AB 두 클래스를 한 클래스 두 `@Nested`로. 케이스 내용 변경 없음(위 세 군데 보정만).
2. `✅ test: B만 참가할 때의 실패 행렬과 어댑터 실패 변환을 검증한다` — B-only 축 신규.

---

작업량은 테스트 파일 한 개 생성 + 두 개 삭제, 케이스 13개 이동 + 5개 신규입니다. 프로덕션 코드는 건드리지 않습니다. 진행할까요?

물어보는 이유는 하나입니다 — ①②를 고치려면 **지금 통과 중인 A 행복 경로 두 케이스의 전제를 바꿔야** 합니다(`ALL_READY` + A 카탈로그만 → `EXPLICIT ready={A}` + 양쪽 카탈로그). 단언 내용은 그대로지만 통과 경로가 달라지니, 리팩터링 중 테스트가 약해지지 않았는지 제가 각 케이스를 옮긴 뒤 **일부러 깨뜨려 보고**(red 확인) 확정하겠습니다. 그 과정까지 포함해서 진행해도 될까요?
````

---

[2026-09-21 03:51:50.038 KST] 사용자

```text
<command-message>gsd-fast</command-message>
<command-name>/gsd-fast</command-name>
<command-args>진행해</command-args>
```

---

[2026-09-21 03:51:50.038 KST] 사용자

```text
Base directory for this skill: C:\Users\kxu45\.claude\skills\gsd-fast

<objective>
Execute a trivial task directly in the current context without spawning subagents
or generating PLAN.md files. For tasks too small to justify planning overhead:
typo fixes, config changes, small refactors, forgotten commits, simple additions.

This is NOT a replacement for /gsd-quick — use /gsd-quick for anything that
needs research, multi-step planning, or verification. /gsd-fast is for tasks
you could describe in one sentence and execute in under 2 minutes.
</objective>

<execution_context>
To load this command's workflow spec: check for `.claude/gsd-core/workflows/fast.md` relative to the current working directory first (project-local); if it is not there, fall back to `~/.claude/gsd-core/workflows/fast.md` (the global install). If neither file exists, stop — a workflow spec is required and none was found.
</execution_context>

<process>
Execute end-to-end.
</process>


ARGUMENTS: 진행해
```

---

[2026-09-21 03:52:25.661 KST] claude code

````text
/gsd-fast의 scope check에 걸립니다. 정직하게 보고합니다.

```
This looks like it needs planning. Use /gsd-quick instead:
  /gsd-quick "공급사 검색 통합 테스트를 참가 조합 축으로 합치고 B-only 축을 추가한다"
```

판단 근거입니다. 파일 조작 자체는 3개(신규 1, 삭제 2)로 경계선이지만, 나머지 기준을 넘습니다.

- **1분 작업이 아닙니다.** 케이스 13개 이동 + 신규 5개, 900줄 가까운 테스트 파일 작성입니다.
- **multi-file refactor입니다.** scope check가 명시적으로 배제하는 유형입니다.
- **단순 적용이 아닙니다.** A 행복 경로 두 케이스의 전제를 바꾸고(`ALL_READY` → `EXPLICIT ready={A}`), 옮긴 케이스마다 red 확인을 해야 합니다.

다만 한 가지는 말씀드리는 게 낫겠습니다. **계획은 이미 직전 대화에서 다 나왔습니다** — 세 `@Nested` 축, 보정할 세 군데(`properties.length()`, 동명이인 `Riverside Hotel Seoul`, `"Old Name"` fixture), B-only 5케이스 목록, 커밋 2개 분리까지요. `/gsd-quick`을 돌리면 그 계획을 처음부터 다시 도출하게 됩니다.

그래서 선택지는 둘입니다.

1. **`/gsd-quick`으로 정식 진행** — GSD 상태 추적과 원자적 커밋 보장을 받되, 계획 단계가 한 번 더 돕니다.
2. **GSD 명령 없이 제가 바로 작업** — 이미 합의된 계획대로 인라인 실행. 커밋은 AGENTS.md 규칙대로 두 개로 나누고, 옮긴 케이스는 red 확인까지 하겠습니다.

어느 쪽으로 할까요? 2번이면 지금 바로 시작합니다.
````

---

[2026-09-21 03:53:41.517 KST] 사용자

```text
2로 가되, 직접 변경함으로써 .planning 문서 내에서 깨지는 부분도 같이 조치해.
```

---

[2026-09-21 03:53:49.129 KST] claude code

```text
2번으로 진행합니다. 먼저 두 파일 전체와 `.planning` 참조를 파악하겠습니다.
```

---

[2026-09-21 03:55:07.383 KST] claude code

```text
이제 합친 파일을 작성합니다.
```

---

[2026-09-21 03:58:10.776 KST] claude code

```text
원인이 분명합니다. 전부 **400**입니다 — 고정 시계(`2026-09-19`)가 적용되지 않아 실제 오늘(`2026-09-21`) 기준으로 `checkIn=2026-09-20`이 과거가 된 겁니다. `@Nested`에서는 바깥 클래스의 `@TestConfiguration` 자동 감지가 걸리지 않습니다. 명시적으로 `@Import`하겠습니다.
```

---

[2026-09-21 03:59:03.318 KST] claude code

```text
18개 전부 통과입니다. 이제 약속한 red 확인을 합니다. 전제가 바뀐 A 행복 경로 두 케이스에서 `A만_참가시킨다()`를 빼 보고, 정말로 그게 load-bearing인지 봅니다.
```

---

[2026-09-21 03:59:53.809 KST] claude code

```text
Red 확인 완료 — 예측한 두 가지가 정확히 나왔습니다.

- `properties.length()` **expected:<2> but was:<3>** → B가 참가해 숙소가 3개
- **`Got a list of values ["A","B"] instead of the expected single value A`** → 제가 예상한 "Riverside Hotel Seoul" 동명이인 충돌

원복하고, 새로 쓴 B 어댑터 직접 단언도 red 확인합니다.
```

---

[2026-09-21 04:00:43.772 KST] claude code

```text
백업은 `$TMPDIR`(`/c/Users/Public/Documents/ESTsoft/CreatorTemp`)에 있었습니다. `cp` 실패로 `&&` 체인이 끊겨 python이 실행되지 않았고, 파일은 직전 red 상태 그대로입니다. 복구 후 B 단언만 뒤집겠습니다.
```

---

[2026-09-21 04:01:25.522 KST] claude code

```text
Red 확인 완료 — `ChunkFailure[supplier=B, reason=INVALID_RESPONSE]`가 **값으로** 돌아온다는 게 실제로 단언되고 있습니다. 원복하고 전체 테스트를 돌립니다.
```

---

[2026-09-21 04:02:35.416 KST] claude code

```text
132개 통과, 실패 0 (기존 127 + 신규 5). 이제 AGENTS.md대로 커밋을 논리 단위로 나누기 위해, 먼저 B-only 축을 뺀 중간 상태로 리팩터링 커밋을 만듭니다.
```

---

[2026-09-21 04:03:16.423 KST] claude code

```text
13개(6+7) — 기존 두 클래스의 케이스 수와 정확히 같습니다. 첫 커밋합니다.
```

---

[2026-09-21 04:04:37.139 KST] claude code

```text
코드 작업 완료입니다. 이제 `.planning`에서 실제로 깨지는 부분을 봅니다.
```

---

[2026-09-21 04:06:16.245 KST] claude code

```text
01은 제 변경과 무관하게 **이미 stale**이었습니다 — v1.0 아카이브가 `.planning/phases/…` → `.planning/milestones/v1.0-phases/…`로 옮기면서 covered_files 경로가 깨져 있었습니다. 제가 깨뜨린 것과 분리해서 보겠습니다.
```

---

[2026-09-21 04:07:44.800 KST] claude code

```text
참조 교정 완료 (남은 두 건은 안내 노트 본문이라 의도된 것). 이제 `covered_digest` 재계산을 시도합니다.
```

---

[2026-09-21 04:08:58.852 KST] claude code

````text
완료했습니다.

## 결과

`StaySearchSupplierIntegrationTest` 하나에 세 축이 나란히 섰습니다. 비대칭이 사라졌습니다.

| 축 | 케이스 |
|---|---|
| 공급사 A만 참가 | 6 (기존 A 클래스 그대로) |
| 공급사 B만 참가 | **5 (신규)** |
| 공급사 A와 B 참가 | 7 (기존 AB 클래스 그대로) |

전체 **132개 통과, 실패 0** (기존 127 + 신규 5).

```
26896d3 ✅ test: B만 참가할 때의 실패 행렬과 어댑터 실패 변환을 검증한다
a8ab6d9 ♻️ refactor: 공급사 검색 통합 테스트를 참가 조합 축으로 합친다
```

## 예상 못 했던 문제 하나

첫 실행에서 **18개 전부 실패**했습니다. 전부 400이었고, 원인은 **`@Nested`에서 바깥 클래스의 `@TestConfiguration`이 자동 감지되지 않는 것**이었습니다. 고정 시계(`2026-09-19`)가 안 붙으니 실제 오늘(`2026-09-21`) 기준으로 `checkIn=2026-09-20`이 과거가 되어 날짜 검증에 걸렸습니다. `@Import(고정시계설정.class)`를 명시해 해결했습니다. 앞 논의에서 "`@NestedTestConfiguration`이 `INHERIT`이라 그대로 물려받는다"고 말씀드렸는데, `@SpringBootTest`·`@DynamicPropertySource`는 물려받지만 **중첩 `@TestConfiguration` 자동 감지는 아니었습니다.**

## Red 확인 — 예측한 그대로 나왔습니다

**① A 행복 경로 두 케이스에서 `A만_참가시킨다()` 제거** → 둘 다 실패, 나머지 16개는 통과(수술이 정확했다는 뜻).

```
JSON path "$.properties.length()" expected:<2> but was:<3>
Got a list of values ["A","B"] instead of the expected single value A
```

숙소 수 3개, 그리고 제가 짚었던 "Riverside Hotel Seoul" 동명이인 충돌. 격리 호출이 load-bearing임이 증명됐습니다.

**② 새 B 어댑터 단언을 `not(empty())` → `empty()`로 뒤집기** → 실패.

```
Expected: an empty collection
     but: <[ChunkFailure[supplier=SupplierId[value=B], reason=INVALID_RESPONSE]]>
```

B 어댑터가 봉투 실패를 `Mono.error`가 아니라 **실패 값으로** 돌려준다는 게 이제 실제로 단언됩니다. 기존 A+B 테스트는 서비스 안전망(`StaySearchService.java:98`)이 덮어도 같은 응답이 나와 구분하지 못하던 공백입니다.

## `.planning` 조치

**제가 깨뜨린 것 — 고쳤습니다.**
- `02-VERIFICATION.md`: `covered_files` 경로 1건 + 본문 인용 6곳
- `03-VERIFICATION.md`: `covered_files` 경로 2건을 1건으로 합침 + 본문 인용 9곳 (`--tests` 명령도 중첩 클래스 경로 `...$공급사_A만_참가`로 갱신)
- 두 파일 제목 아래에 재배치 안내 노트를 넣었습니다. 케이스 수는 검증 당시 값이라는 점도 적었습니다.

**제가 깨뜨리지 않은 것 — 손대지 않았습니다.**
- PLAN / SUMMARY / REVIEW / CONTEXT / PATTERNS / tdd-red JSON의 클래스명 참조는 그대로 뒀습니다. 당시 작업을 기록한 문서라 이름을 바꾸면 기록이 거짓이 됩니다.

**따로 보고드릴 것이 있습니다.** `covered_digest`를 재계산하려 했는데 실패했습니다.

```
Error: could not compute fingerprint — a covered file is missing, unreadable, or escapes the project root
```

원인은 제 변경이 아니라 **v1.0 아카이브 때 생긴 기존 문제**입니다. 아카이브가 `.planning/phases/…` → `.planning/milestones/v1.0-phases/…`로, `.planning/REQUIREMENTS.md` → `.planning/milestones/v1.0-REQUIREMENTS.md`로 옮겼는데 `covered_files`의 경로는 옛 위치 그대로입니다. **네 페이즈 모두** 해당되고, 01과 04는 제가 건드린 파일이 하나도 없는데도 `stale`입니다. 즉 제 작업 전부터 이미 전부 stale이었습니다.

그래서 02·03도 여전히 `stale`입니다. 제 참조 교정은 "문서를 읽는 사람이 코드를 찾아갈 수 있게" 한 것이지, 신선도를 되살린 건 아닙니다. 되살리려면 네 페이즈의 `covered_files` 아카이브 경로를 일괄 교정해야 하는데, 이건 제가 깨뜨린 범위 밖이라 손대지 않았습니다. 원하시면 별건으로 처리하겠습니다.

## 커밋 안 된 것

`.planning`은 AGENTS.md대로 커밋하지 않았습니다. 그 외 추적되지 않는 파일:

- `docs/설계 논의사항/chunk-all-excluded-failure.md` — 앞서 쓴 논의 노트
- `.gsd/dispatch-isolation-sentinel.json`
- `docs/ai-logs/` 신규 3건 — 작업 중에 직접 추가하신 것으로 보입니다. AI 활용 기록은 사용자 몫이라 건드리지 않았습니다.

그리고 `origin/main` 기준으로 이제 **64커밋** 앞서 있습니다.
````
